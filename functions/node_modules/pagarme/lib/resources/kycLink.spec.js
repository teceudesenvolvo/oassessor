import { merge } from 'ramda'
import runTest from '../../test/runTest'

const findOptions = {
  connect: {
    api_key: 'ak_test_dev',
  },
  body: {
    api_key: 'ak_test_dev',
  },
}

test('client.kycLink.create', () =>
  runTest(merge(findOptions, {
    subject: client => client.kycLink.create({
      recipientId: 're_1638449629355',
    }),
    url: '/recipients/re_1638449629355/kyc_link',
    method: 'POST',
  }))
)
